# Ankara — Dependencies
# Install: pip install -r requirements.txt

# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18
pydantic-settings==2.7.1

# --- ML / Deep Learning ---
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
timm>=1.0.0
sentence-transformers>=3.0.0

# --- Computer Vision ---
opencv-python-headless>=4.9.0

# --- RAG ---
chromadb>=0.5.0
llama-index>=0.11.0

# --- PDF processing (for paper ingestion) ---
pymupdf>=1.24.0

# --- Forensic reasoning ---
# Local LLM via Ollama (https://ollama.com) — uses the HTTP API, no Python
# client required. Optional cloud fallbacks:
# anthropic>=0.39.0
# openai>=1.50.0

# --- Apple Silicon optimization (macOS only) ---
# pip install mlx mlx-lm coremltools

# --- Dev / Testing ---
pytest>=8.0.0
httpx>=0.27.0
